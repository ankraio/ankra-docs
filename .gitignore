node_modules/
.mintlify/
.DS_Store
*.log

# Local MR-review worktrees (git worktree checkouts of other branches).
.mr-worktrees/
# Claude Code agent worktrees (ephemeral, never commit)
.claude/worktrees/

# The cluster route census, fetched by CI and copied locally (scripts/check_api_paths.py)
routes.json
