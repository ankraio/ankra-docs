apiVersion: v1
kind: Service
metadata:
  name: ankra-docs
  labels:
    app.kubernetes.io/name: ankra-docs
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ankra-docs
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
