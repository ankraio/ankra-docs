---
title: "ankra stack-profiles"
description: "Manage reusable stack profiles"
---

{/* Generated by ankra-cli tools/gendocs — do not edit by hand. */}

## ankra stack-profiles

Manage organisation-level stack profiles: create them from deployed stacks, list and describe them, apply them to clusters, version and share them, and export or import them as infrastructure-as-code.

## ankra stack-profiles apply

Apply (instantiate) a stack profile onto a cluster.

By default this creates a reviewable stack DRAFT on the target cluster - nothing is
deployed until you review it in the Ankra dashboard or pass --deploy.

Bind profile parameters with --set name=value. For secret parameters prefer
--set-file name=path or --set-env name=ENV_VAR so the value never appears in your
shell history or process list. Use 'ankra stack-profiles get &lt;profile-id&gt;' to see
which parameters a profile expects.

```bash
ankra stack-profiles apply [profile-id|profile-name] [flags]
```

**Flags**

| Flag | Default | Description |
|------|---------|-------------|
| `--cluster` |   | Target cluster name or ID (defaults to the selected cluster) |
| `--deploy` | `false` | Deploy the stack immediately instead of leaving a draft for review |
| `--dry-run` | `false` | Show the value every input would deploy with (your --set, the selected choice, or the default) without creating anything |
| `-o`, `--output` |   | Output format: json or yaml (default: human-readable) |
| `--set` | `[]` | Bind a parameter: name=value (repeatable; not for secrets) |
| `--set-env` | `[]` | Bind a parameter from an environment variable: name=ENV_VAR (repeatable; secret-safe) |
| `--set-file` | `[]` | Bind a parameter from a file: name=path (repeatable; secret-safe) |
| `--stack-name` |   | Name for the new stack (defaults to the profile's stack name) |
| `--version` |   | Profile version to apply, as 1 or v1 (defaults to the profile's current version) |

## ankra stack-profiles create

Create a stack profile by snapshotting a deployed cluster stack as
version 1. The source stack's add-ons and manifests become the profile
contents; detected inputs become the profile's parameters.

```bash
ankra stack-profiles create [flags]
```

**Examples**

```bash
ankra stack-profiles create --name postgres-ha --stack postgres --cluster staging \
    --category database --tag postgresql --tag ha --description "Production PostgreSQL"
```

**Flags**

| Flag | Default | Description |
|------|---------|-------------|
| `--category` | `general` | Profile category (e.g. 'database') |
| `--changelog` |   | Changelog note for version 1 |
| `--cluster` |   | Source cluster name or ID (defaults to the selected cluster) |
| `--description` |   | Profile description shown in the catalogue |
| `--include-addon-configurations` | `true` | Carry the source add-ons' values into the profile |
| `--logo-url` |   | Logo image URL shown in the catalogue |
| `--name` |   | Profile name (required) |
| `-o`, `--output` |   | Output format: json or yaml (default: human-readable) |
| `--stack` |   | Source stack name to snapshot (required) |
| `--tag` | `[]` | Catalogue tag (repeatable) |
| `--visibility` |   | Profile visibility: organisation (default) or public |

## ankra stack-profiles delete

Delete a stack profile. Stacks already deployed from it keep running;
the profile stops appearing in the catalogue and can no longer be applied.

```bash
ankra stack-profiles delete [profile-id|profile-name] [flags]
```

**Flags**

| Flag | Default | Description |
|------|---------|-------------|
| `-o`, `--output` |   | Output format: json or yaml (default: human-readable) |
| `--yes` | `false` | Skip the confirmation prompt |

## ankra stack-profiles demo

Launch a demo of a profile on the staging cluster to see what it actually
installs - no cluster of your own required. A demo installs the profile's
add-ons and manifests into one throwaway namespace, quota-bounded so it
cannot crowd the staging cluster, and cleans itself up automatically.

## ankra stack-profiles demo detail

Show a demo's workloads and readiness

```bash
ankra stack-profiles demo detail [profile-id|profile-name] <workspace-id> [flags]
```

**Flags**

| Flag | Default | Description |
|------|---------|-------------|
| `-o`, `--output` |   | Output format: json or yaml (default: human-readable) |

## ankra stack-profiles demo launch

Launch a demo of a profile on the staging cluster

```bash
ankra stack-profiles demo launch [profile-id|profile-name] [flags]
```

**Examples**

```bash
ankra stack-profiles demo launch postgres-ha
  ankra stack-profiles demo launch postgres-ha --version v2 --ttl-hours 4 --set replicas=1
```

**Flags**

| Flag | Default | Description |
|------|---------|-------------|
| `-o`, `--output` |   | Output format: json or yaml (default: human-readable) |
| `--set` | `[]` | Bind a parameter: name=value (repeatable; not for secrets) |
| `--set-env` | `[]` | Bind a parameter from an environment variable: name=ENV_VAR (repeatable; secret-safe) |
| `--set-file` | `[]` | Bind a parameter from a file: name=path (repeatable; secret-safe) |
| `--ttl-hours` | `0` | Hours before the demo stops itself (defaults server-side) |
| `--version` |   | Profile version to demo, as 1 or v1 (defaults to the current version) |

## ankra stack-profiles demo list

List the live demos of a profile

```bash
ankra stack-profiles demo list [profile-id|profile-name] [flags]
```

**Flags**

| Flag | Default | Description |
|------|---------|-------------|
| `-o`, `--output` |   | Output format: json or yaml (default: human-readable) |

## ankra stack-profiles demo logs

Read a demo's pod logs

```bash
ankra stack-profiles demo logs [profile-id|profile-name] <workspace-id> [flags]
```

**Flags**

| Flag | Default | Description |
|------|---------|-------------|
| `-o`, `--output` |   | Output format: json or yaml (default: human-readable) |

## ankra stack-profiles demo stop

Stop a demo and clean up its namespace

```bash
ankra stack-profiles demo stop [profile-id|profile-name] <workspace-id> [flags]
```

**Flags**

| Flag | Default | Description |
|------|---------|-------------|
| `-o`, `--output` |   | Output format: json or yaml (default: human-readable) |

## ankra stack-profiles deployments

List every stack your organisation deployed from this profile: the target
cluster, the stack, the profile version it runs, and whether a newer
version is available.

```bash
ankra stack-profiles deployments [profile-id|profile-name] [flags]
```

**Flags**

| Flag | Default | Description |
|------|---------|-------------|
| `-o`, `--output` |   | Output format: json or yaml (default: human-readable) |

## ankra stack-profiles diff

Compare two published versions of a profile

```bash
ankra stack-profiles diff [profile-id|profile-name] [flags]
```

**Examples**

```bash
ankra stack-profiles diff postgres-ha --from v1 --to v2
  ankra stack-profiles diff postgres-ha --from 1 --to 3 -o json
```

**Flags**

| Flag | Default | Description |
|------|---------|-------------|
| `--from` |   | Version to compare from, as 1 or v1 (required) |
| `-o`, `--output` |   | Output format: json or yaml (default: human-readable) |
| `--to` |   | Version to compare to, as 1 or v1 (required) |

## ankra stack-profiles drafts

Work with stack profile builder drafts from the terminal.

A draft is the editable working copy behind a profile version: open one from
an existing profile (or from a deployed stack), annotate its parameters with
the titles and descriptions the launch form shows as guidance, and publish it
as the next version.

## ankra stack-profiles drafts annotate

Set a parameter's title and description on a draft. The description is
the guidance the launch form shows under the field, so this is how a profile
author instructs the person filling it in. Publishing the draft makes the
annotations live.

```bash
ankra stack-profiles drafts annotate <draft-id> [flags]
```

**Flags**

| Flag | Default | Description |
|------|---------|-------------|
| `--add` | `false` | Declare the input if the draft does not have it, e.g. a choice that sets other inputs and is referenced by no manifest |
| `--default` |   | Default value (pass an empty string to clear it) |
| `--description` |   | Guidance shown under the field in the launch form |
| `--enum` |   | Allowed values, comma separated (implies --type enum) |
| `-o`, `--output` |   | Output format: json or yaml (default: human-readable) |
| `--parameter` |   | Parameter name to annotate |
| `--required` | `false` | Whether the input must be set when the profile is used (--required=false to relax) |
| `--title` |   | Display title for the field (optional) |
| `--type` |   | Input type: string, number, boolean or enum |

## ankra stack-profiles drafts create

Open a builder draft: --profile edits an existing profile (publishing
creates its next version), --name starts a brand-new profile, and
--source-cluster with --source-stack seeds the draft from a deployed stack.

```bash
ankra stack-profiles drafts create [flags]
```

**Flags**

| Flag | Default | Description |
|------|---------|-------------|
| `--name` |   | Name for a brand-new profile draft |
| `-o`, `--output` |   | Output format: json or yaml (default: human-readable) |
| `--profile` |   | Existing profile (name or ID) to open a draft on |
| `--source-cluster` |   | Cluster (name or ID) to seed the draft from a deployed stack |
| `--source-stack` |   | Deployed stack name to seed the draft from (with --source-cluster) |

## ankra stack-profiles drafts delete

Discard a builder draft

```bash
ankra stack-profiles drafts delete <draft-id> [flags]
```

**Flags**

| Flag | Default | Description |
|------|---------|-------------|
| `-o`, `--output` |   | Output format: json or yaml (default: human-readable) |

## ankra stack-profiles drafts get

Show a draft's parameters and their annotations

```bash
ankra stack-profiles drafts get <draft-id> [flags]
```

**Flags**

| Flag | Default | Description |
|------|---------|-------------|
| `-o`, `--output` |   | Output format: json or yaml (default: human-readable) |

## ankra stack-profiles drafts list

List the organisation's open builder drafts

```bash
ankra stack-profiles drafts list [flags]
```

**Flags**

| Flag | Default | Description |
|------|---------|-------------|
| `-o`, `--output` |   | Output format: json or yaml (default: human-readable) |

## ankra stack-profiles drafts options

Give a draft's input choices, where each choice also answers other inputs.

One "Model size" input with a choice of 8b or 32b can then move the model id,
the context length and the volume size together, so whoever applies the
profile picks one thing instead of keeping four consistent. At apply time the
platform resolves a choice in a fixed order: a value passed with --set, then
what the selected choice sets, then the input's own default.

A choice can set any non-secret input that does not offer choices of its own.
Declare a choice input that no manifest references with
'drafts annotate &lt;draft-id&gt; --parameter &lt;name&gt; --add --type enum'.

## ankra stack-profiles drafts options remove

Remove one choice from an input

```bash
ankra stack-profiles drafts options remove <draft-id> [flags]
```

**Flags**

| Flag | Default | Description |
|------|---------|-------------|
| `-o`, `--output` |   | Output format: json or yaml (default: human-readable) |
| `--parameter` |   | Input the choice belongs to |
| `--value` |   | Value of the choice to remove |

## ankra stack-profiles drafts options set

Add or update one choice on an input

```bash
ankra stack-profiles drafts options set <draft-id> [flags]
```

**Flags**

| Flag | Default | Description |
|------|---------|-------------|
| `--description` |   | Why someone would pick this choice (optional) |
| `-o`, `--output` |   | Output format: json or yaml (default: human-readable) |
| `--parameter` |   | Input the choice belongs to |
| `--set` | `[]` | Input this choice answers: name=value (repeatable) |
| `--title` |   | Label shown for the choice (optional) |
| `--unset` | `[]` | Stop this choice answering an input: name (repeatable) |
| `--value` |   | Value the input takes when this choice is picked |

## ankra stack-profiles drafts publish

Publish a draft as a profile version

```bash
ankra stack-profiles drafts publish <draft-id> [flags]
```

**Flags**

| Flag | Default | Description |
|------|---------|-------------|
| `--changelog` |   | Changelog entry for the version |
| `--channel` | `stable` | Release channel for the version |
| `-o`, `--output` |   | Output format: json or yaml (default: human-readable) |
| `--visibility` |   | Profile visibility applied at publish (organisation or public) |

## ankra stack-profiles drafts rebase

Move a draft's base to the profile's latest published version. A draft
opened before someone else published cannot be published until it is
rebased; the draft's contents are kept and the upstream changes reported.

```bash
ankra stack-profiles drafts rebase <draft-id> [flags]
```

**Flags**

| Flag | Default | Description |
|------|---------|-------------|
| `-o`, `--output` |   | Output format: json or yaml (default: human-readable) |

## ankra stack-profiles drafts submit-suggestion

Submit a draft that edits another organisation's public profile as a
suggestion. The owning organisation reviews it with
'ankra stack-profiles suggestions'; submitting retires the draft.

```bash
ankra stack-profiles drafts submit-suggestion <draft-id> [flags]
```

**Flags**

| Flag | Default | Description |
|------|---------|-------------|
| `-o`, `--output` |   | Output format: json or yaml (default: human-readable) |
| `--title` |   | One line describing the change (required) |

## ankra stack-profiles drafts validate

Run the publish validations on a draft without publishing

```bash
ankra stack-profiles drafts validate <draft-id> [flags]
```

**Flags**

| Flag | Default | Description |
|------|---------|-------------|
| `-o`, `--output` |   | Output format: json or yaml (default: human-readable) |

## ankra stack-profiles export-iac

Export a profile version as ClusterInfrastructureAsCode YAML

```bash
ankra stack-profiles export-iac [profile-id|profile-name] [flags]
```

**Flags**

| Flag | Default | Description |
|------|---------|-------------|
| `-o`, `--output` |   | Write YAML to this file instead of stdout |
| `--version` |   | Profile version to export, as 1 or v1 (defaults to the profile's current version) |

## ankra stack-profiles get

Describe a stack profile: its metadata, published versions, and the parameters you can bind when applying it.

```bash
ankra stack-profiles get [profile-id|profile-name] [flags]
```

**Flags**

| Flag | Default | Description |
|------|---------|-------------|
| `-o`, `--output` |   | Output format: json or yaml (default: human-readable) |
| `--version` |   | Profile version to describe, as 1 or v1 (defaults to the current version) |

## ankra stack-profiles import

Import a profile from a ClusterInfrastructureAsCode YAML file

```bash
ankra stack-profiles import [file] [flags]
```

**Flags**

| Flag | Default | Description |
|------|---------|-------------|
| `--category` | `general` | Profile category |
| `--name` |   | Profile name (defaults to metadata.name in the file) |

## ankra stack-profiles list

List stack profiles in the active organisation

```bash
ankra stack-profiles list [flags]
```

**Flags**

| Flag | Default | Description |
|------|---------|-------------|
| `--category` |   | Filter profiles by category (e.g. 'monitoring') |
| `-o`, `--output` |   | Output format: json or yaml (default: human-readable) |
| `--page` | `1` | Page number |
| `--page-size` | `25` | Page size |
| `--search` |   | Filter profiles by name |

## ankra stack-profiles logo

Manage the uploaded logo a profile shows in the catalogue. Uploading
stores the image bytes with Ankra and clears any logo URL; to reference an
image by URL instead, use 'ankra stack-profiles update --logo-url'.

## ankra stack-profiles logo clear

Remove a profile's uploaded logo

```bash
ankra stack-profiles logo clear [profile-id|profile-name] [flags]
```

**Flags**

| Flag | Default | Description |
|------|---------|-------------|
| `-o`, `--output` |   | Output format: json or yaml (default: human-readable) |

## ankra stack-profiles logo get

Download the uploaded logo's image bytes. Without --output the file is
named after the profile reference with the served image type's extension.

```bash
ankra stack-profiles logo get [profile-id|profile-name] [flags]
```

**Flags**

| Flag | Default | Description |
|------|---------|-------------|
| `--output` |   | Write the image to this file (defaults to &lt;profile&gt;&lt;ext&gt;) |

## ankra stack-profiles logo set

Upload a logo image for a profile you own. PNG, JPEG, and WebP are
accepted, at most 512 KiB; large images are resized automatically and
square images look best. The upload clears any stored logo URL.

```bash
ankra stack-profiles logo set [profile-id|profile-name] <image-file> [flags]
```

**Examples**

```bash
ankra stack-profiles logo set postgres-ha ./logo.png
```

**Flags**

| Flag | Default | Description |
|------|---------|-------------|
| `-o`, `--output` |   | Output format: json or yaml (default: human-readable) |

## ankra stack-profiles save-version

Save a new profile version by re-snapshotting a deployed stack. The new
version becomes the profile's current version.

```bash
ankra stack-profiles save-version [profile-id|profile-name] [flags]
```

**Examples**

```bash
ankra stack-profiles save-version postgres-ha --stack postgres --cluster staging \
    --changelog "Bump CloudNativePG to 1.30"
```

**Flags**

| Flag | Default | Description |
|------|---------|-------------|
| `--changelog` |   | Changelog note for the new version |
| `--channel` | `stable` | Release channel for the new version |
| `--cluster` |   | Source cluster name or ID (defaults to the selected cluster) |
| `--include-addon-configurations` | `true` | Carry the source add-ons' values into the version |
| `-o`, `--output` |   | Output format: json or yaml (default: human-readable) |
| `--stack` |   | Source stack name to snapshot (required) |

## ankra stack-profiles set-current-version

Set which published version new deployments of this profile resolve to.
Rolling back is setting the pointer to an earlier version.

```bash
ankra stack-profiles set-current-version [profile-id|profile-name] <version> [flags]
```

**Examples**

```bash
ankra stack-profiles set-current-version postgres-ha v3
```

**Flags**

| Flag | Default | Description |
|------|---------|-------------|
| `-o`, `--output` |   | Output format: json or yaml (default: human-readable) |

## ankra stack-profiles share

Share a profile with specific organisations. Shared organisations can view
and deploy every version but cannot change the profile. Ask the other
organisation for their slug - admins find it under organisation settings.

## ankra stack-profiles share add

Share a profile with another organisation

```bash
ankra stack-profiles share add [profile-id|profile-name] <organisation-slug> [flags]
```

**Examples**

```bash
ankra stack-profiles share add postgres-ha acme-corp
```

**Flags**

| Flag | Default | Description |
|------|---------|-------------|
| `-o`, `--output` |   | Output format: json or yaml (default: human-readable) |

## ankra stack-profiles share list

List the organisations a profile is shared with

```bash
ankra stack-profiles share list [profile-id|profile-name] [flags]
```

**Flags**

| Flag | Default | Description |
|------|---------|-------------|
| `-o`, `--output` |   | Output format: json or yaml (default: human-readable) |

## ankra stack-profiles share remove

Revoke one share grant by its id (from 'share list'). The organisation
loses access to the profile; stacks it already deployed keep running.

```bash
ankra stack-profiles share remove [profile-id|profile-name] <share-id> [flags]
```

**Flags**

| Flag | Default | Description |
|------|---------|-------------|
| `-o`, `--output` |   | Output format: json or yaml (default: human-readable) |

## ankra stack-profiles suggestions

Review the cross-organisation suggestions submitted against a profile.
Approving one publishes its frozen contents as the profile's next version.
Withdraw is the proposer's verb for a suggestion their organisation submitted.

## ankra stack-profiles suggestions approve

Approve a suggestion, publishing it as the profile's next version

```bash
ankra stack-profiles suggestions approve [profile-id|profile-name] <suggestion-id> [flags]
```

**Flags**

| Flag | Default | Description |
|------|---------|-------------|
| `--changelog` |   | Changelog note (defaults to one composed from the suggestion) |
| `--channel` | `stable` | Release channel for the published version |
| `-o`, `--output` |   | Output format: json or yaml (default: human-readable) |

## ankra stack-profiles suggestions get

Show one suggestion's contents and upstream drift

```bash
ankra stack-profiles suggestions get [profile-id|profile-name] <suggestion-id> [flags]
```

**Flags**

| Flag | Default | Description |
|------|---------|-------------|
| `-o`, `--output` |   | Output format: json or yaml (default: human-readable) |

## ankra stack-profiles suggestions list

List the suggestions visible to your organisation on a profile

```bash
ankra stack-profiles suggestions list [profile-id|profile-name] [flags]
```

**Flags**

| Flag | Default | Description |
|------|---------|-------------|
| `-o`, `--output` |   | Output format: json or yaml (default: human-readable) |

## ankra stack-profiles suggestions reject

Reject a suggestion, optionally with a note the proposer can read

```bash
ankra stack-profiles suggestions reject [profile-id|profile-name] <suggestion-id> [flags]
```

**Flags**

| Flag | Default | Description |
|------|---------|-------------|
| `--note` |   | Resolution note the proposer can read |
| `-o`, `--output` |   | Output format: json or yaml (default: human-readable) |

## ankra stack-profiles suggestions withdraw

Withdraw a suggestion your organisation submitted

```bash
ankra stack-profiles suggestions withdraw [profile-id|profile-name] <suggestion-id> [flags]
```

**Flags**

| Flag | Default | Description |
|------|---------|-------------|
| `-o`, `--output` |   | Output format: json or yaml (default: human-readable) |

## ankra stack-profiles update

Update how a stack profile appears in the catalogue: its name,
description, category, tags, logo URL, and visibility. Only the flags you
set change; pass an empty value to clear a field.

```bash
ankra stack-profiles update [profile-id|profile-name] [flags]
```

**Examples**

```bash
ankra stack-profiles update postgres-ha --description "Production PostgreSQL 17" --tag postgresql --tag cnpg
  ankra stack-profiles update postgres-ha --visibility public
```

**Flags**

| Flag | Default | Description |
|------|---------|-------------|
| `--category` |   | New category |
| `--description` |   | New description (empty clears) |
| `--logo-url` |   | New logo image URL (empty clears) |
| `--name` |   | New profile name |
| `-o`, `--output` |   | Output format: json or yaml (default: human-readable) |
| `--tag` | `[]` | New tag set (repeatable; replaces the stored tags) |
| `--visibility` |   | Profile visibility: organisation or public |

## ankra stack-profiles version

Show one published version's contents and parameters

```bash
ankra stack-profiles version [profile-id|profile-name] <version> [flags]
```

**Flags**

| Flag | Default | Description |
|------|---------|-------------|
| `-o`, `--output` |   | Output format: json or yaml (default: human-readable) |

